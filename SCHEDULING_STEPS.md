# 排程步驟說明（含場地數與一鍵排程）

主辦方不需匯入賽程檔案，只要依下列步驟設定，即可用「一鍵排程」自動為所有比賽分配時段。演算法會考慮**場地數（每時段可同時進行幾場）**與**不可出賽**。

---

## 一、整體流程概覽

1. **場地管理**（排程頁）→ 建立幾個場地（例如 2 個球場）
2. **每週時段模板**（排程頁）→ 定義每週哪些時段可打，並設定「同時可進行場數」
3. **所有可用時段**（排程頁）→ 依模板生成整個賽季的日曆時段
4. **不可出賽**（選手頁）→ 為各隊伍／選手設定每週不能打的時段
5. **產生對戰**（賽程頁或籤表）→ 產生比賽
6. **一鍵排程**（排程頁）→ 自動分配每場比賽到時段

---

## 二、各步驟說明

### 步驟 1：場地管理（排程頁）

- 路徑：**Admin → 排程（Scheduling）→ 場地管理**
- 建立此賽事會用到的場地（例如「Court 1」「Court 2」）。
- **場地數**會影響後面「每時段可排幾場」：
  - 做法 A：在**時段模板**或**可用時段**裡，把該時段的「同時可進行場數」設成場地數（例如 2），則一個時段可排 2 場。
  - 做法 B：同一個時段為每個場地各建一筆「可用時段」（同一日期時間、不同場地），每筆 capacity 設 1，則一鍵排程會自動把不同比賽排到不同場地。

### 步驟 2：每週比賽時段模板（排程頁）

- 路徑：**排程頁 → 每週比賽時段模板**
- 定義「每週哪些時段可以排比賽」（例如週一 18:00–20:00、週三 18:00–20:00）。
- **同時可進行場數（capacity）**：表示這個時段最多可同時打幾場比賽（通常等於場地數）。例如 2 個場地就設 2；若不設，預設為 1。
- 同一套時段會出現在**選手頁「不可出賽」**的下拉選單，讓隊伍勾選「不能打」的時段。

### 步驟 3：所有可用時段（排程頁）

- 路徑：**排程頁 → 所有可用時段**
- 用「依模板生成時段」選**套用日期（開始／結束）**，一次產生整個賽季的實際日曆時段。
- 每個產生的時段會帶入模板的 **capacity**（同時可進行場數）；之後一鍵排程會依此限制：**同一時段最多排 capacity 場**。
- 若有少數特別日期要調整，可手動新增或刪除個別時段。

### 步驟 4：不可出賽（選手頁）

- 路徑：**Admin → 選手（Players）**
- 在「不可出賽時段上限」與各隊伍／選手的「設定」中，勾選每週**不能打**的時段（與排程頁的時段模板同一套）。
- 一鍵排程會避開這些時段，不會把該隊伍／選手排進去。

### 步驟 5：產生對戰（賽程頁／籤表）

- 依賽制在賽程頁或籤表頁產生比賽（例如單淘汰、季賽等）。
- 此時比賽尚未指定時段（slot_id / scheduled_time 為空）。

### 步驟 6：一鍵排程（排程頁）

- 路徑：**排程頁 → 一鍵排程**
- **兩種方式**：
  - **預覽排程（可拖曳編輯）**（建議）：演算法先產生建議排程並開啟預覽畫面，可**拖曳**比賽到不同時段或「未排入」，確認後按「儲存排程」才寫入。
  - **直接排程（不預覽）**：不預覽，直接寫入並導向賽程頁。
- 可設定「**同一隊伍至少間隔 N 個時段**」與「**同一隊伍至少隔 N 天**」再排下一場：
  - **時段間隔**：0=可連續、1=至少隔 1 時段、2=至少隔 2 時段。
  - **日曆天間隔**：0=可連日、1=至少隔 1 天（不連日）、2=至少隔 2 天。**多場地時建議設「至少隔 1 天」**，否則同一隊伍可能被排在連續兩天的同一時段（例如 3/2 場B、3/3 場B），因演算法只數「時段個數」不數「日曆天」。
- 演算法規則：
  - 依**時間先後**為每場比賽找**第一個**可用時段。
  - **場地數（capacity）**：每個時段最多排 `capacity` 場。
  - **不可出賽**：跳過該隊伍／選手不能打的時段。
  - **不重複**：同一時段內同一隊伍／選手只排一場。
  - **休息間隔**：依上述設定，同一隊伍不會在連續 N+1 個時段內出賽，且不會在連續日曆日出賽（當「至少隔 N 天」> 0 時）。
- 若勾選「清除既有排程後重排」，會先清空所有比賽的時段再重新分配。
- **賽程頁 → 排程編輯（拖曳）**：以「時段 × 場地」格子手動拖放比賽；若放入的時段為某隊不可出賽會顯示**橘色警示**，仍可儲存由主辦決定。
- 若有比賽無法排入，請增加「所有可用時段」或調整不可出賽後再執行一次。

---

## 三、場地數與 capacity 對應關係（整理）

| 情境 | 建議設定 |
|------|----------|
| 1 個場地 | 時段模板／可用時段不設 capacity 或設 1；每個時段排 1 場。 |
| 2 個場地 | 時段模板「同時可進行場數」設 2；生成後的每個時段可排 2 場。 |
| 要區分「哪一場在哪個場地」 | 同一日期時間為每個場地各建一筆「可用時段」（各 capacity 1），排程會自動分到不同 slot（不同場地）。 |

---

## 四、常見問題

- **部分比賽沒排到？**  
  可能時段總量不足或該時段都被不可出賽佔滿。請增加「所有可用時段」或放寬不可出賽，再執行一次一鍵排程。

- **不想清掉已手動排好的比賽？**  
  不要勾選「清除既有排程後重排」；演算法只會為**尚未指定時段**的比賽排程。

- **沒有匯入檔可以排嗎？**  
  可以。只要完成上述步驟 1～5，在排程頁用「一鍵排程」即可，不需匯入賽程檔案。

- **想先看結果再決定要不要存？**  
  使用「預覽排程（可拖曳編輯）」：會跳出預覽畫面，可拖曳比賽到不同時段或取消排程，確認後再按「儲存排程」。

- **按「儲存排程」沒反應或顯示找不到 API？**  
  請確認專案已包含並部署 `app/api/admin/events/[eventId]/auto-schedule/apply/` 路由（`apply/route.ts`）。若此資料夾未加入版本控制，部署後會 404，需執行 `git add` 並提交後重新部署。
