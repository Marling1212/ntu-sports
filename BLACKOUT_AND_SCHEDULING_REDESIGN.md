# 不可出賽時段與排程功能重整規劃

## 一、你的需求整理

- **比賽以周為單位排定**，每個隊伍/選手可設定「每週絕對無法出賽」的幾天或時段（例如 A 隊周一、週三不能，B 隊周二、週四不能 → A vs B 只能排週五）。
- **不可出賽屬於隊伍/選手的資料**，希望從 Scheduling 頁面移到 **Players 頁面** 管理。
- **Scheduling 頁面太亂**，希望重新整理、職責分明。
- **排程演算法**（怎麼根據黑名單把比賽排進時段）之後再做，**先做好資料結構與管理**。

---

## 二、目前資料結構（維持不變即可）

| 資料表 | 用途 |
|--------|------|
| `team_blackout_templates` | **每週**不可出賽模板：`event_id`, `player_id`, `day_of_week`, `start_time`, `end_time`。表示「該隊伍每週的星期幾、哪個時段不能打」。 |
| `team_blackouts` | **實際**不可出賽時段：`event_id`, `player_id`, `start_time`, `end_time`（含日期）。由「模板 + 日期區間」批量生成。 |
| `events.blackout_limit` | 每個隊伍每週最多可填幾筆不可出賽（選填，不填＝不限制）。 |

目前結構已經支援「以周為單位、每隊選每週哪幾天/哪幾個時段不能打」，不需改 DB schema，只要調整**介面位置與流程**。

---

## 三、建議的功能歸屬與流程

### 1. Players 頁面（隊伍/選手資料）

**新增／移入的內容：**

- **不可出賽時段上限**  
  - 放在活動設定或 Players 頁頂說明即可（例如：「每隊每週最多可填 X 個不可出賽時段」），或保留在 Settings，由你決定。
- **每隊／每人的「每週不可出賽」設定（核心）**  
  - 以**隊伍／選手為單位**操作：
    - 選擇一個隊伍/選手 → 為他新增「每週星期幾 + 時段」的不可出賽模板（對應 `team_blackout_templates`）。
    - 可顯示：該活動的「每週比賽時段模板」（來自 Scheduling）作為選項，例如勾選「週一 18:00–20:00」「週三 18:00–20:00」表示這兩段不能打。
  - 介面建議：
    - 在選手列表或隊伍列表旁/下，每個隊伍有一塊「不可出賽」區塊，或
    - 獨立一個子區「不可出賽設定」，用下拉選隊伍後，底下列出該隊的每週不可出賽模板（星期幾 + 時段），可新增/刪除。
- **（可選）由模板生成實際不可出賽**  
  - 在 Players 頁提供「依日期區間，從每週模板生成實際 `team_blackouts`」的按鈕，方便一次產生整學期黑名單；或這步也可以留在 Scheduling 當「排程前準備」。

這樣一來，**不可出賽的「資料」完全在 Players 頁**：誰、每週哪幾天/哪幾個時段不能打，都在這裡維護。

### 2. Scheduling 頁面（場地、時段、排程）

**只保留與「場地、時段、排程」直接相關的內容：**

- **場地管理**  
  - 維持：新增/編輯/刪除場地（`event_courts`）。
- **每週比賽時段模板**  
  - 維持：主辦方定義「每週哪些時段可以排比賽」（例如週一 18:00–20:00、週三 18:00–20:00、週五 14:00–17:00），對應 `event_slot_templates`。  
  - 這會是 Players 頁「不可出賽」設定時的參考選項（同一套時段）。
- **所有可用時段（日曆時段）**  
  - 維持：由模板生成或手動新增的 `event_slots`（實際日期 + 時間），之後排程會用這些 slot 來排比賽。
- **從 Scheduling 頁面移除**  
  - 不可出賽時段上限的編輯（可改到 Settings 或 Players 頁頂）。  
  - 隊伍不可出賽模板的新增/編輯/刪除（改到 Players 頁）。  
  - 依模板生成 `team_blackouts` 的 UI 可選：留在 Scheduling 當「排程前一步」或移到 Players 頁。

重整後，Scheduling 的快速導航可以只保留：**場地管理 → 每週時段模板 → 可用時段**（未來再加「排程結果」等）。

---

## 四、建議的實作順序（先資料與管理）

1. **Phase 1：把「不可出賽」管理移到 Players 頁**
   - 在 Players 頁新增一個區塊（例如「不可出賽設定」或每個隊伍底下的子區）。
   - 從 SchedulingManager 抽出與 `team_blackout_templates` / `team_blackouts` 相關的 state 與 API，做成獨立元件（例如 `PlayerBlackoutManager` 或 `TeamBlackoutSection`）。
   - 在 Players 頁依「隊伍/選手」維度顯示與編輯：
     - 每週不可出賽模板（星期幾 + 時段），
     - 若你要，也可顯示「已生成的實際 blackouts」唯讀列表（或只顯示筆數）。
   - 需要讀取 `event_slot_templates`（或精簡後的「每週可打時段」清單）讓主辦方用同一套時段來勾選「不能打」的時段。

2. **Phase 2：Scheduling 頁精簡**
   - 從 SchedulingManager 移除「不可出賽時段上限」與「隊伍不可出賽」的 UI。
   - 保留並整理：場地、每週時段模板、可用時段；快速導航與區塊標題對應上述三塊。
   - 若「依日期區間生成 team_blackouts」保留在 Scheduling，可改成精簡的一小區（只選日期 + 一鍵生成），並註解說明：實際「誰不能打」已在 Players 頁設定。

3. **Phase 3（之後）：排程演算法**
   - 使用 `event_slots`、`team_blackouts`、對戰組合，產出「每場比賽建議的 slot」或直接寫入 matches 的 `scheduled_time` / `slot_id`，這部分你之後再設計即可。

---

## 五、Players 頁「不可出賽」介面草圖（可再簡化）

```
[ 選手列表 / 隊伍列表 ]（現有）

--- 不可出賽設定 ---
說明：以周為單位，每隊可設定每週哪些時段絕對無法出賽，排程時會避開這些時段。

• 每隊每週不可出賽上限：[ 2 ] 筆（選填，留空=不限制）  [儲存]  ← 可放這裡或 Settings

選擇隊伍/選手：[下拉選 A 隊 / B 隊 / ...]

  A 隊 的每週不可出賽：
  ┌─────────────────────────────────────────────┐
  │ 週一 18:00–20:00  [刪除]                     │
  │ 週三 18:00–20:00  [刪除]                     │
  └─────────────────────────────────────────────┘
  [ + 新增一筆 ]  → 彈窗：選擇「星期幾 + 時段」（可依 event_slot_templates 選）

[ 依日期區間生成實際不可出賽 ]  [開始日期] [結束日期] [生成]  ← 可選，或放到 Scheduling
```

- 資料來源：`team_blackout_templates`（依 `player_id` 篩選）、`event_slot_templates`（供選時段）。
- 新增/刪除都寫回 `team_blackout_templates`；若要做「生成實際 blackouts」，再呼叫現有邏輯寫入 `team_blackouts`。

---

## 六、小結

| 項目 | 建議 |
|------|------|
| 不可出賽時段 | **歸屬 Players 頁**，以隊伍/選手為單位管理「每週哪幾天/哪幾個時段不能打」。 |
| 資料結構 | **維持現有** `team_blackout_templates`、`team_blackouts`、`blackout_limit`。 |
| Scheduling 頁 | **只保留** 場地、每週時段模板、可用時段；不可出賽的編輯全部移到 Players。 |
| 排程演算法 | 之後再做；先完成上述資料與管理動線。 |

若你同意這方向，下一步可以從 **Phase 1（在 Players 頁加上不可出賽區塊並從 Scheduling 抽離相關 UI）** 開始實作；若要改文案或欄位命名也可以一起調整。
